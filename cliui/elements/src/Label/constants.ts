/** Tag name used to register the built-in label component. */
export const LABEL_TAG_NAME = 'label';

/** Observed custom element attributes for the built-in label. */
export const LABEL_OBSERVED_ATTRIBUTES = ['for', 'disabled'];
