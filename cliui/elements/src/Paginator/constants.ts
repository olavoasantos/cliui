/** Tag name used to register the built-in paginator component. */
export const PAGINATOR_TAG_NAME = 'paginator';

/** Observed custom element attributes for the built-in paginator. */
export const PAGINATOR_OBSERVED_ATTRIBUTES = ['page', 'total-pages'];
