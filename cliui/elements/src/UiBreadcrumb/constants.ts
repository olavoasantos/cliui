/** Tag name used to register the built-in breadcrumb segment. */
export const UI_BREADCRUMB_TAG_NAME = 'ui-breadcrumb';

/** Observed custom element attributes for the built-in breadcrumb segment. */
export const UI_BREADCRUMB_OBSERVED_ATTRIBUTES = ['href'];
