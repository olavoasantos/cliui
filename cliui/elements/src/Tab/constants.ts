/** Tag name used to register the built-in tab component. */
export const TAB_TAG_NAME = 'tab';

/** Observed custom element attributes for the built-in tab. */
export const TAB_OBSERVED_ATTRIBUTES = ['title', 'disabled'];
