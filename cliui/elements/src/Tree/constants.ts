/** Tag name used to register the built-in tree view component. */
export const TREE_TAG_NAME = 'tree';

/** Observed custom element attributes for the built-in tree view. */
export const TREE_OBSERVED_ATTRIBUTES: string[] = [];

/** Character used as the indicator when a tree item is collapsed. */
export const TREE_INDICATOR_COLLAPSED = '▸';

/** Character used as the indicator when a tree item is expanded. */
export const TREE_INDICATOR_EXPANDED = '▾';

/** Character used as the leaf indicator (non-expandable). */
export const TREE_INDICATOR_LEAF = ' ';

/** Number of columns to indent per nesting level. */
export const TREE_INDENT_SIZE = 2;
