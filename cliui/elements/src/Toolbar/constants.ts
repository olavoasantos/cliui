/** Tag name used to register the built-in toolbar component. */
export const TOOLBAR_TAG_NAME = 'toolbar';

/** Observed custom element attributes for the built-in toolbar. */
export const TOOLBAR_OBSERVED_ATTRIBUTES: string[] = [];
