/** Tag name used to register the built-in image component. */
export const IMG_TAG_NAME = 'img';

/** Observed custom element attributes for the built-in image element. */
export const IMG_OBSERVED_ATTRIBUTES = ['src', 'alt', 'width', 'height'];
