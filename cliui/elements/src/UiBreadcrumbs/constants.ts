/** Tag name used to register the built-in breadcrumbs container. */
export const UI_BREADCRUMBS_TAG_NAME = 'ui-breadcrumbs';

/** Observed custom element attributes for the built-in breadcrumbs. */
export const UI_BREADCRUMBS_OBSERVED_ATTRIBUTES = ['separator'];

/** Default separator character between breadcrumb segments. */
export const DEFAULT_UI_BREADCRUMBS_SEPARATOR = '›';
