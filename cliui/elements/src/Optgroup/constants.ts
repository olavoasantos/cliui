/** Tag name used to register the built-in optgroup component. */
export const OPTGROUP_TAG_NAME = 'optgroup';

/** Observed custom element attributes for the built-in optgroup. */
export const OPTGROUP_OBSERVED_ATTRIBUTES = ['label', 'disabled'];
