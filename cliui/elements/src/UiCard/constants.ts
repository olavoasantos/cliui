/** Tag name used to register the built-in card component. */
export const UI_CARD_TAG_NAME = 'ui-card';

/** Observed custom element attributes for the built-in card. */
export const UI_CARD_OBSERVED_ATTRIBUTES = ['header', 'footer'];
