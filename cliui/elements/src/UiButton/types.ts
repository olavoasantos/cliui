/** Supported visual variants for the built-in button component. */
export type UiButtonVariant = 'primary' | 'secondary';

/** Supported semantic tones for the built-in button component. */
export type UiButtonTone = 'default' | 'dangerous';
