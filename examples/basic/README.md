# Basic example

Run from the repository root:

```bash
pnpm --dir examples/basic start
```

What you should see:

- the terminal switches to the alternate screen
- a styled box renders with a title and message
- press `q` to quit and restore the terminal
