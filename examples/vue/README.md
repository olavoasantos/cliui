# Vue example

Demonstrates `@micra/terminal-dom` with Vue 3 — SFC components, Composition API, and event handling.

```bash
pnpm --dir examples/vue dev
```

Press any key to increment the counter. `Ctrl+C` to quit.
