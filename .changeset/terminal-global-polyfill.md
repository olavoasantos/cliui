---
"@cliui/terminal": minor
---

Add global environment polyfill for framework compatibility

Polyfill global scope with DOM classes (document, window, navigator, etc.) so UI frameworks (React, Preact, Solid, Vue, Svelte) can run in the terminal without code changes. Zero-config detection and patching at import time.
