---
'@micra/terminal-dom': minor
---

Propagate `<a href>` to cell hyperlink field for OSC 8 terminal links (COMP-2). Anchor elements with an `href` attribute now emit clickable hyperlinks in supporting terminal emulators.
