---
"@cliui/terminal": minor
---

Add br, wbr, and hr element handling

`<br>` forces a line break in text layout. `<wbr>` inserts a word-break opportunity. `<hr>` renders as a horizontal rule spanning the element width using box-drawing characters.
