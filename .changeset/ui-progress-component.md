---
'@micra/terminal-dom': minor
---

Add built-in `<ui-progress>` custom element with proportional rendering, spring-based animation, customizable glyphs, and cross-terminal-consistent default preset
