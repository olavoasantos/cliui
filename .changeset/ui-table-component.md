---
'@micra/terminal-dom': minor
---

Add built-in table component (`<ui-table>`) with HTML-inspired child elements (`ui-thead`, `ui-tbody`, `ui-tfoot`, `ui-tr`, `ui-th`, `ui-td`) for structured tabular data with automatic column alignment
