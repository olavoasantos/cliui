---
"@cliui/dom": minor
---

Add HTMLStyleElement

`<style>` element that notifies the style engine when its text content changes. Integrates with the hooks bridge to trigger stylesheet re-parsing on DOM mutations.
