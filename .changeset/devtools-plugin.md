---
"@cliui/devtools": minor
---

Add devtools() terminal plugin

Zero-config integration: `devtools()` returns a terminal plugin that starts the CDP server and logs the DevTools connection URL. Drop-in usage: `terminal.use(devtools())`.
