---
"@cliui/devtools": minor
---

Add Network domain with fetch interception

Intercept `fetch()` and `http`/`https` requests to display them in the DevTools Network tab. Capture request headers, response status, body content, and timing information.
