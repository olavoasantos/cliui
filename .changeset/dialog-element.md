---
'@micra/terminal-dom': minor
---

Add HTMLDialogElement with modal/non-modal open, close, focus trapping, and Escape handling (COMP-9). The `<dialog>` element is hidden by default and visible when open, with `showModal()` providing focus trapping and Escape-to-close semantics.
