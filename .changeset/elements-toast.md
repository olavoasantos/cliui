---
"@cliui/elements": minor
---

Add Toast component

Toast notification with auto-dismiss timer. Supports `info`, `success`, `warning`, and `error` tone variants. Configurable duration via attribute.
