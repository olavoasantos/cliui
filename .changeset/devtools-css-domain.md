---
"@cliui/devtools": minor
---

Add CSS domain handler

Style inspection with getMatchedStylesForNode returning matched rules with specificity and source ranges. getComputedStyleForNode for resolved values. Inline style editing with setStyleTexts. Source range tracking across minified stylesheets.
