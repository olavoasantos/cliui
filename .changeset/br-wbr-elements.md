---
'@micra/terminal-dom': minor
---

Handle `<br>` and `<wbr>` elements in the layout engine (COMP-3). `<br>` forces a hard line break in text content; `<wbr>` inserts a zero-width break opportunity.
