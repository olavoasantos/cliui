---
"@cliui/terminal": minor
---

Add user-agent stylesheet

Built-in stylesheet providing default styles for semantic HTML elements at user-agent cascade priority. Includes styles for `<a>` (underline + link color), `<strong>`, `<em>`, `<code>`, `<hr>`, headings, and registered component styles. User CSS overrides these without specificity concerns.
