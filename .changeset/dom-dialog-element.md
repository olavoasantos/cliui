---
"@cliui/dom": minor
---

Add HTMLDialogElement with modal support

`<dialog>` element with showModal(), close(), open attribute, returnValue, focus trapping, Escape key handling, and ::backdrop pseudo-element. Modal dialogs center in the viewport and trap tab focus within their content.
