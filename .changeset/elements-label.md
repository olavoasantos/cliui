---
"@cliui/elements": minor
---

Add Label component

`<label>` with `for` attribute that forwards focus to the referenced form element on click. Prevents mousedown default so focus is correctly transferred to the target element.
