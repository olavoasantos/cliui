---
"@cliui/terminal": minor
---

Add terminal capability detection

Detect terminal color support (truecolor, 256-color, 16-color, no-color), synchronized output (mode 2026), and graphics protocol support (Kitty, iTerm2). Capability detection runs before first render and adapts output accordingly.
