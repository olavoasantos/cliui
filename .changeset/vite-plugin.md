---
"@cliui/vite-plugin": minor
---

Add Vite plugin for terminal-dom development

Vite dev mode with CSS hot-module replacement — stylesheet changes apply without restarting the terminal app. Production build mode generating standalone Node.js output. Resolves `@cliui/terminal` from the project root. Silences Vite's browser-oriented logger.
