---
"@cliui/devtools": minor
---

Add Chrome DevTools Protocol bridge

WebSocket server for CDP communication with discovery endpoint. Node registry mapping DOM elements to stable integer IDs with CDP-compatible serialization. CDP transport layer with message routing and session management. Stable target ID for reconnection across restarts.
