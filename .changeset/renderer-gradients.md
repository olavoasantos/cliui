---
"@cliui/terminal": minor
---

Add linear-gradient support for backgrounds and borders

`linear-gradient()` CSS function support in `background-color` and border color properties. Proper 2D gradient sampling across the element's cell grid. Rotating gradient angles for animated effects.
