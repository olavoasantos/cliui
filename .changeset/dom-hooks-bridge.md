---
"@cliui/dom": minor
---

Add DOM hooks bridge

Hooks system for observing DOM mutations (attribute changes, child list changes, text content changes) without tight coupling to the rendering engine. Used by the style engine and DevTools bridge to react to DOM changes.
