---
"@cliui/elements": minor
---

Add Select and Option components

`<select>` dropdown with keyboard navigation (Arrow Up/Down, Enter to select, Escape to close), typeahead search, `<option>` with value and selected attributes, and `<optgroup>` for grouped options. Select listbox caps at configurable max-visible-options with overflow scrolling.
