---
"@cliui/elements": minor
---

Add Tabs component

`<tabs>` container with `<tab title="...">` children. Visual tab bar rendered from title attributes. Arrow Left/Right keyboard navigation between tabs. Mouse click on tab headers. Disabled tab skipping. Only the active tab panel is visible.
