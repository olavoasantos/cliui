---
"@cliui/terminal": minor
---

Add CLI runner

`npx @cliui/terminal <file.html>` CLI entry point for running HTML terminal apps. Supports `--no-alt-screen`, `--fps`, and `--help` flags. Loads HTML documents, resolves resources from the filesystem, and starts the terminal render loop.
