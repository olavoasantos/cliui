---
"@cliui/devtools": minor
---

Add DOM domain handler

Tree inspection with getDocument, requestChildNodes, and querySelector. Live DOM editing with setAttributeValue, removeAttribute, setOuterHTML, and removeNode. Bridge DOM mutations to CDP events via hooks for real-time tree updates in DevTools. getBoxModel for element geometry. getNodeForLocation for inspect-element mode with two-pass hit-testing and tolerance snapping.
