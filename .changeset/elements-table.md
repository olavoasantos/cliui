---
"@cliui/elements": minor
---

Add Table component

`<table>`, `<thead>`, `<tbody>`, `<tr>`, `<th>`, `<td>` elements with column width alignment, header styling, bordered table variant, and content-box sizing for correct text rendering with borders.
