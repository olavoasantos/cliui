---
'@micra/terminal-dom': minor
---

Support color gradients on borders via `linear-gradient()` in `border-color`. Colors interpolate smoothly per-cell along each edge. Also introduces `parseCSSFunction` as a generic CSS function parser shared by `var()` and `linear-gradient()`.
