---
"@cliui/terminal": minor
---

Add custom border styles via @border-style at-rules

Define custom border characters with `@border-style` CSS at-rules. Each rule specifies the eight border segments (top, right, bottom, left, and four corners) as Unicode characters. Custom styles are referenced by name in the `border-style` property.
