---
"@cliui/elements": minor
---

Add Message component

Inline message with `info`, `success`, `warning`, and `error` tone variants. Visual styling via the `tone` attribute with distinct colors per variant.
