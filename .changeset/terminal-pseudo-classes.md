---
"@cliui/terminal": minor
---

Add hover support with CSS pseudo-classes

Mouse enter/leave tracking with `:hover` pseudo-class. CSS pseudo-class support for interactive states: `:hover`, `:focus`, `:active`, `:disabled`, `:checked`. Pseudo-class changes trigger style recomputation on affected elements.
