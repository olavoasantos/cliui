---
"@cliui/elements": minor
---

Add Menu and Dropdown components

`<menu>` with `<menu-item>` children supporting keyboard navigation (Arrow Up/Down, Enter to select, Escape to close). `<dropdown>` trigger that opens a positioned menu. Click-outside-to-close and focus return on menu close.
