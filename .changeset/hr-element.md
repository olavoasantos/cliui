---
'@micra/terminal-dom': minor
---

Render `<hr>` as a horizontal line of `─` characters spanning the element width (COMP-4). The Painter detects `<hr>` elements and fills their content area with box-drawing horizontal line characters.
