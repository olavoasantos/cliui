---
"@cliui/dom": minor
---

Add Performance API

PerformanceEntry, PerformanceMark, PerformanceMeasure, PerformanceEventTiming, and paint timing entries (first-paint, first-contentful-paint). Performance class with mark(), measure(), getEntries(), getEntriesByType(), and getEntriesByName(). PerformanceObserver with microtask-batched delivery and entryTypes/type filtering.
