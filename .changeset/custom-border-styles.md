---
'@micra/terminal-dom': minor
---

Support custom border character sets via `@border-style` CSS at-rules. Define a named border style with character declarations in `<style>` blocks, then reference it with `border-style: <name>`.
