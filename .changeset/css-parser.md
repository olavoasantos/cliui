---
"@cliui/terminal": minor
---

Add CSS parser with selector AST

Hand-written CSS parser producing a selector AST with support for element, class, ID, attribute, pseudo-class, and combinators (descendant, child, adjacent sibling, general sibling). Parsed selectors are reused across style resolution passes.
