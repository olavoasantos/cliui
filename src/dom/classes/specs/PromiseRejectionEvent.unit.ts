import {describe, it} from 'vitest';

import {PromiseRejectionEvent} from '../PromiseRejectionEvent';

describe('PromiseRejectionEvent', () => {
  describe('constructor', () => {
    it.todo('creates an event with the given type');
    it.todo('sets promise from eventInitDict');
    it.todo('sets reason from eventInitDict');
  });
});
