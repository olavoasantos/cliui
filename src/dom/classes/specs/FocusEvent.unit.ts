import {describe, it} from 'vitest';

import {FocusEvent} from '../FocusEvent';

describe('FocusEvent', () => {
  describe('constructor', () => {
    it.todo('creates an event with the given type');
    it.todo('sets relatedTarget from eventInitDict');
    it.todo('defaults relatedTarget to null when omitted');
  });
});
