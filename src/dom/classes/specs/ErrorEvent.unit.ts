import {describe, it} from 'vitest';

import {ErrorEvent} from '../ErrorEvent';

describe('ErrorEvent', () => {
  describe('constructor', () => {
    it.todo('creates an event with the given type');
    it.todo('sets message from eventInitDict');
    it.todo('sets filename from eventInitDict');
    it.todo('sets lineno from eventInitDict');
    it.todo('sets colno from eventInitDict');
    it.todo('sets error from eventInitDict');
  });
});
