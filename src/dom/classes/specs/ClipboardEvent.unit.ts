import {describe, it} from 'vitest';

import {ClipboardEvent} from '../ClipboardEvent';

describe('ClipboardEvent', () => {
  describe('constructor', () => {
    it.todo('creates an event with the given type');
    it.todo('sets clipboardData from eventInitDict');
    it.todo('defaults clipboardData to null when omitted');
  });
});
