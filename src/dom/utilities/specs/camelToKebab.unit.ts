import {describe, it} from 'vitest';

import {camelToKebab} from '../camelToKebab';

describe('camelToKebab', () => {
  it.todo('converts a single uppercase letter');
  it.todo('converts multiple uppercase letters');
  it.todo('returns lowercase strings unchanged');
  it.todo('handles leading uppercase for vendor prefixes');
  it.todo('handles empty string');
});
