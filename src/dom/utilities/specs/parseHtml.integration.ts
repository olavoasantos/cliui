import {describe, it} from 'vitest';

describe('parseHtml integration', () => {
  it.todo(
    'parses markup into a document fragment that can be queried, styled, and serialized without losing structure',
  );
});
