import {describe, it} from 'vitest';

import {ParentNodeGuard} from '../ParentNodeGuard';

describe('ParentNodeGuard', () => {
  it.todo('returns true for an element node');
  it.todo('returns true for a document fragment');
  it.todo('returns false for a text node');
  it.todo('returns false for a comment node');
});
