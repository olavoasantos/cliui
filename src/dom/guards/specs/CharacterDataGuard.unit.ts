import {describe, it} from 'vitest';

import {CharacterDataGuard} from '../CharacterDataGuard';

describe('CharacterDataGuard', () => {
  it.todo('returns true for a text node');
  it.todo('returns true for a comment node');
  it.todo('returns false for an element node');
  it.todo('returns false for a document fragment');
});
