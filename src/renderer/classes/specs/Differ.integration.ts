import {describe, it} from 'vitest';

describe('Differ integration', () => {
  it.todo(
    'produces minimal terminal updates for consecutive frame buffers with mixed content and style changes',
  );
});
