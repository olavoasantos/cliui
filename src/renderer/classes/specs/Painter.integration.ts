import {describe, it} from 'vitest';

describe('Painter integration', () => {
  it.todo(
    'paints nested layout boxes into the cell buffer with borders, clipping, background fills, and text content',
  );
});
