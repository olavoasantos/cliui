import {describe, it} from 'vitest';

describe('Terminal', () => {
  it.todo('normalizes constructor options and wires default runtime dependencies');
  it.todo('does not start the terminal loop more than once');
  it.todo('cleans up timers, input listeners, and terminal state when exiting');
});
