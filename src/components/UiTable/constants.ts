/** Tag name used to register the built-in table component. */
export const UI_TABLE_TAG_NAME = 'ui-table';

/** Observed custom element attributes for the built-in table. */
export const UI_TABLE_OBSERVED_ATTRIBUTES = ['column-gap'];

/** Default gap between columns in cells. */
export const DEFAULT_UI_TABLE_COLUMN_GAP = 2;
