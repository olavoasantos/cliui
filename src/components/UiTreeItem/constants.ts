/** Tag name used to register the built-in tree item component. */
export const UI_TREE_ITEM_TAG_NAME = 'ui-tree-item';

/** Observed custom element attributes for the built-in tree item. */
export const UI_TREE_ITEM_OBSERVED_ATTRIBUTES = ['expandable', 'open', 'value', 'highlighted'];
