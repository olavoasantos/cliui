/** Tag name used to register the built-in details component. */
export const UI_DETAILS_TAG_NAME = 'ui-details';

/** Observed custom element attributes for the built-in details. */
export const UI_DETAILS_OBSERVED_ATTRIBUTES = ['open', 'disabled'];

/** Character used as the indicator when collapsed. */
export const UI_DETAILS_INDICATOR_COLLAPSED = '▸';

/** Character used as the indicator when expanded. */
export const UI_DETAILS_INDICATOR_EXPANDED = '▾';
