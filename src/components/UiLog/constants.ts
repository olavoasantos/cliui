/** Tag name used to register the built-in log viewer component. */
export const UI_LOG_TAG_NAME = 'ui-log';

/** Observed custom element attributes for the built-in log viewer. */
export const UI_LOG_OBSERVED_ATTRIBUTES = ['max-lines'];

/** Default maximum number of retained lines. 0 means unlimited. */
export const DEFAULT_UI_LOG_MAX_LINES = 0;
