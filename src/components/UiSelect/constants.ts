/** Tag name used to register the built-in select component. */
export const UI_SELECT_TAG_NAME = 'ui-select';

/** Observed custom element attributes for the built-in select. */
export const UI_SELECT_OBSERVED_ATTRIBUTES = ['value', 'disabled', 'open'];

/** Character used as the dropdown indicator in the trigger. */
export const UI_SELECT_INDICATOR = '▾';

/** Z-index for the dropdown listbox overlay. */
export const UI_SELECT_LISTBOX_Z_INDEX = 10;
