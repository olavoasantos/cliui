/** Tag name used to register the built-in code block component. */
export const UI_CODEBLOCK_TAG_NAME = 'ui-codeblock';

/** Observed custom element attributes for the built-in code block. */
export const UI_CODEBLOCK_OBSERVED_ATTRIBUTES = ['language', 'theme', 'line-numbers', 'wrap'];

/** Default Shiki theme name used when no theme attribute is provided. */
export const DEFAULT_UI_CODEBLOCK_THEME = 'nord';

/** CSS class for a code block line wrapper. */
export const UI_CODEBLOCK_LINE_CLASS = 'ui-codeblock-line';

/** CSS class for the line number gutter. */
export const UI_CODEBLOCK_GUTTER_CLASS = 'ui-codeblock-gutter';

/** CSS class for the line content area. */
export const UI_CODEBLOCK_CONTENT_CLASS = 'ui-codeblock-content';
