/** Tag name used to register the built-in meter component. */
export const UI_METER_TAG_NAME = 'ui-meter';

/** Observed custom element attributes for the built-in meter. */
export const UI_METER_OBSERVED_ATTRIBUTES = ['value', 'min', 'max', 'low', 'high', 'optimum'];

/** Default bar fill character. */
export const UI_METER_FILL_CHAR = '█';

/** Default bar empty character. */
export const UI_METER_EMPTY_CHAR = '░';

/** Default meter bar width in columns. */
export const DEFAULT_UI_METER_WIDTH = 20;
