/** Tag name used to register the built-in confirmation component. */
export const UI_CONFIRMATION_TAG_NAME = 'ui-confirmation';

/** Observed custom element attributes for the built-in confirmation. */
export const UI_CONFIRMATION_OBSERVED_ATTRIBUTES = ['message', 'confirm-label', 'cancel-label'];

/** Default label for the confirm button. */
export const DEFAULT_UI_CONFIRMATION_CONFIRM_LABEL = 'Confirm';

/** Default label for the cancel button. */
export const DEFAULT_UI_CONFIRMATION_CANCEL_LABEL = 'Cancel';
