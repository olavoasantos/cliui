/** Tag name used to register the built-in diff viewer component. */
export const UI_DIFF_TAG_NAME = 'ui-diff';

/** Observed custom element attributes for the built-in diff viewer. */
export const UI_DIFF_OBSERVED_ATTRIBUTES: string[] = [];

/** Color for added lines. */
export const UI_DIFF_ADDED_COLOR = 'green';

/** Color for removed lines. */
export const UI_DIFF_REMOVED_COLOR = 'red';

/** Color for context lines. */
export const UI_DIFF_CONTEXT_COLOR = '#888888';
