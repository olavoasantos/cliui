/** Tag name used to register the built-in prompt component. */
export const UI_PROMPT_TAG_NAME = 'ui-prompt';

/** Observed custom element attributes for the built-in prompt. */
export const UI_PROMPT_OBSERVED_ATTRIBUTES = ['message', 'confirm-label', 'cancel-label'];

/** Default label for the confirm button. */
export const DEFAULT_UI_PROMPT_CONFIRM_LABEL = 'OK';

/** Default label for the cancel button. */
export const DEFAULT_UI_PROMPT_CANCEL_LABEL = 'Cancel';

/** Default width of the prompt input field. */
export const DEFAULT_UI_PROMPT_INPUT_WIDTH = 30;
