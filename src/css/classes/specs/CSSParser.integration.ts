import {describe, it} from 'vitest';

describe('CSSParser integration', () => {
  it.todo(
    'parses a stylesheet with nested at-rules, comments, and multiple selector blocks into declarations consumed by the style pipeline',
  );
});
