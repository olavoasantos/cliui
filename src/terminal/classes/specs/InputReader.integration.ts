import {describe, it} from 'vitest';

describe('InputReader integration', () => {
  it.todo(
    'translates raw terminal byte sequences into keyboard, mouse, and paste events when attached to a readable input stream',
  );
});
