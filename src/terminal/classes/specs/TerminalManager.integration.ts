import {describe, it} from 'vitest';

describe('TerminalManager integration', () => {
  it.todo(
    'enables terminal modes, negotiates capabilities, and restores the terminal state on shutdown',
  );
});
